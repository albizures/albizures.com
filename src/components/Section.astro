---
export type Props = {
	title?: string
}

const { title } = Astro.props;
---

<section class="py-6">
	{title && <h2 class="text-2xl font-semibold tracking-wide">{title}</h2>}
	<div class="mt-2">
		<slot />
	</div>
</section>
